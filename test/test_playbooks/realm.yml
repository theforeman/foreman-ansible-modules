---
- hosts: localhost
  tasks:
  - name: Load server config
    include_vars:
      file: server_vars.yml
  - include: tasks/realm.yml
    vars:
      realm_state: present
  - include: tasks/realm.yml
    vars:
      realm_state: absent
