language: python
python:
  - "2.6"
  - "3.5"
install:
  - "pip install --upgrade pip"
  - "pip install pep8"
  - "pip install -r requirements.txt"
before_script: "pep8 --ignore=E402 --max-line-length=160 ."
