---
- hosts: tests
  collections:
    - theforeman.foreman
  gather_facts: false
  vars_files:
    - vars/server.yml
  tasks:
    - include_tasks: tasks/content_export_library.yml
      vars:
        expected_change: true
        organization_name: "Default Organization"
        foreman_organization: "Default Organization"
        destination_server: "library-export.example.com"
    - include_tasks: tasks/content_export_version.yml
      vars:
        expected_change: true
        organization_name: "Default Organization"
        foreman_organization: "Default Organization" # required for CV publish
        destination_server: "library-export.example.com"
        id: 32
        content_view: "My content view"
    - include_tasks: tasks/content_export_repository.yml
      vars:
        expected_change: true
        organization_name: "Default Organization"
        id: 31
...
