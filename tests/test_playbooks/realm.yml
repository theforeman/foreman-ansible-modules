---
- hosts: tests
  gather_facts: false
  vars_files:
    - vars/server.yml
  tasks:
  - include: tasks/realm.yml
    vars:
      realm_state: present
  - include: tasks/realm.yml
    vars:
      realm_state: absent
