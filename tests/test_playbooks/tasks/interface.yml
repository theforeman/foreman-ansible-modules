---
- name: "Ensure host '{{ interface_name }}' is {{ interface_state }}"
  foreman_interface:
    username: "{{ foreman_username }}"
    password: "{{ foreman_password }}"
    server_url: "{{ foreman_server_url }}"
    validate_certs: "{{ foreman_validate_certs }}"
    host: "{{ host_name }}"
    name: "{{ interface_name }}"
    primary: "{{ interface_primary | default(omit) }}"
    mac: "{{ interface_mac | default(omit) }}"
    ip: "{{ interface_ip | default(omit) }}"
    type: "{{ interface_type | default(omit) }}"
    subnet: "{{ subnet_name | default(omit) }}"
    domain: "{{ domain_name | default(omit) }}"
    identifier: "{{ interface_identifier | default(omit) }}"
    managed: "{{ interface_managed | default(omit) }}"
    primary: "{{ interface_primary | default(omit) }}"
    provision: "{{ interface_provision | default(omit) }}"
    virtual: "{{ interface_virtual | default(omit) }}"
    tag: "{{ interface_tag | default(omit) }}"
  register: result
- assert:
    fail_msg: "Ensuring interface is {{ interface_state }} failed! (expected_change: {{ expected_change | default('unknown') }})"
    that:
      - result.changed == expected_change
  when: expected_change is defined
  vars:
    interface_state: present
...
