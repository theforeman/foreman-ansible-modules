---
- hosts: tests
  collections:
    - theforeman.foreman
  gather_facts: false
  vars_files:
    - vars/server.yml
  tasks:
    - include_tasks: tasks/content_export_library.yml
      vars:
        organization_name: "Export Org"
        foreman_organization: "Export Org"
        destination_server: "airgapped.example.com"
    - include_tasks: tasks/content_export_info.yml
...
